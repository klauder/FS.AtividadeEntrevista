@model WebAtividadeEntrevista.Models.ClienteModel

@{
    ViewBag.Title = "Cadastrar Cliente";
}

@section scripts{

    <script>
        var urlPost = '@Url.Action("Incluir", "Cliente", new { area = "" })';
        var obj = @Html.Raw(Json.Encode(Model));
        var urlRetorno = '@Url.Action("Index", "Cliente", new { area = "" })'; //Caso necessite voltar para a listagem de Clientes, após a inclusão
    </script>

    @Scripts.Render("~/bundles/clientes")
}

<h2>@Html.Raw(ViewBag.Title)</h2>

@Html.Partial("Forms")